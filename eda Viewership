#Impoty Library

# Importing a python libray for data manipulation 
import pandas as pd

#Importing a python library for numerical operations
import numpy as n

 #creating path of the excel file
data_path="/Workspace/Users/mbali.sandav@gmail.com/Viewership Analysis .xlsx" 

# Installing dependency called openpyxl
!pip install openpyxl

# storing the excel file in variable and read the file
df=pd.read_excel(data_path)

#checking the last 5 rows 
df.tail(5)

#Shows the number of rows and columns
df.shape

# Shows summary for numerical columns in the DataFrame
df.describe()

# Gives the column names in the dataset
df.columns

# Gives the number of unique values in each column
df.nunique()

# shows you all the unique values that appear in the column called “Platform” in the dataset.
df['Platform'].unique()

# shows you all the unique values that appear in the column called “PlayEventType” in the dataset.
df['PlayEventType'].unique()

# It gives the summary information about the dataset
df.info()

# Shows the number of missing values in each column
df.isnull().sum()

#To check the type of data in the dataset
df.dtypes

# To check duplicated values by rows
df.duplicated()

# This code counts how many times each type of Platform shows up in the data.
df['Platform'].value_counts()

# Calculate the VideoTitle with the highest total watch duration
viwership_analysis.groupby('VideoTitle')['TotalTimeWatched'].sum().sort_values(ascending=False)

# Calculates how many unique customers viewed each VideoTitle in descending order
df.groupby('VideoTitle')['CustomerID'].nunique().sort_values(ascending=False)






















